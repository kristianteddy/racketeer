import {DarkThemeToggle, RangeSlider} from "flowbite-react";

export default async function Home() {
    return (
        <main className="flex max-w-screen-xl min-h-screen mx-auto items-center justify-center dark:bg-gray-800">

        </main>
    );
}
